#pragma once
#include "../../BASE.h"


class L_287_FindDuplicate : public BASE {
public:
	virtual void main()
	{
	}
	int findDuplicate(vector<int>& nums) {
		/*
		int count = accumulate(nums.begin(), nums.end(), 0);
		int n = nums.size();
		return count - n * (n - 1) / 2;
		*/
	}
};